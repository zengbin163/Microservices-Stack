spring:
   cloud:
      bus:
         refresh:
            enabled: true
      enabled: true
   kafka:
      bootstrap-servers: kafka-host.com:9092
      consumer:
         group-id: config-group

management:
   endpoints:
      web:
         exposure:
            include: '*'